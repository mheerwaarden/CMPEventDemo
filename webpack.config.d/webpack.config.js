devServer: {
  host: '0:0:0:0', // Listen on all available network interfaces; alternatively, specify IP address
  port: 8080,
  disableHostCheck: true // Allow external connections, can be risky in production
}